#!/usr/bin/env bash
# Double-clickable launcher for the capture menu.
# Put this file anywhere convenient (Desktop is fine).
# One-time setup in Terminal:  chmod +x ~/Desktop/Capture.command
exec "$HOME/recordings/capture"
